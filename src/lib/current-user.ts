// TODO: Replace with actual auth when added.
// For now, the "current user" is stored in a cookie set by the dashboard user switcher.

export const CURRENT_USER_COOKIE = "starflight_current_user_id";
